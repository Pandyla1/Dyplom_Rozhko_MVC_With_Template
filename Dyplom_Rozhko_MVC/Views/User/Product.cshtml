@model Dyplom_Rozhko_MVC.Models.ConnectAllTables

@{
    Layout = "~/Views/Shared/_LayoutPageOther.cshtml";
    var selectedProduct = Model.Product.FirstOrDefault();
}

<div class="container-fluid py-5">
    <div class="row px-xl-5">
        <div class="col-lg-5 pb-5">
            <div id="product-carousel" class="carousel slide" data-ride="carousel">
                <div class="carousel-item active">
                    <img class="w-100 h-100" src="@Url.Content(selectedProduct.ImageUrl)" alt="Image" style="border: 2px solid #ecf4fc;">
                </div>
            </div>
        </div>

        <div class="col-lg-7 pb-5">
            <h3 class="font-weight-semi-bold">@selectedProduct.ProductName</h3>
            <h3 style="margin-bottom: 20px; margin-top: 30px;" class="font-weight-semi-bold mb-4">@selectedProduct.Price₴</h3>
            <p class="mb-4">@selectedProduct.ShortDescription</p>
            <div class="d-flex align-items-center mb-4 pt-2">
                <button class="btn btn-primary px-3">
                    <a class="text-dark" href="@Url.Action("AddToCart", new { productId = ViewBag.Id, toWishlist = false })">Додати в кошик</a>
                </button>
                <button style="margin-left:10px;" class="btn btn-primary px-3">
                    <a class="text-dark" href="@Url.Action("AddToWishlist", new { productId = ViewBag.Id })">Додати в бажане</a>
                </button>
            </div>
            <div style="margin-top: 30px" class="tab-pane fade show active">
                <h5 class="mb-3">Характеристики товару</h5>
                <div class="row">
                    <table style="margin-left: 15px;" border="1">
                        <tbody>
                            <tr>
                                <td align="center" style="width: 100px; height: 35px;">Розмір</td>
                                <td align="center" style="width: 100px; height: 35px;">@selectedProduct.Size</td>
                            </tr>
                            <tr>
                                <td align="center" style="width: 100px; height: 35px;">Колір</td>
                                <td align="center" style="width: 100px; height: 35px;">@selectedProduct.Color</td>
                            </tr>
                            <tr>
                                <td align="center" style="width: 100px; height: 35px;">Виробник</td>
                                <td align="center" style="width: 100px; height: 35px;">@selectedProduct.CompanyName</td>
                            </tr>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
<!-- Shop Detail End -->